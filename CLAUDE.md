# 本项目的开发规矩

这份文件对本仓库里的**每一次变更**生效，不分大小。写给未来的我自己（以及任何 AI 助手）。

## 为什么有这份文件

这个项目做到一半时，前 12 个任务严格执行了「实装 → 规格评审 → 质量评审」，该机制发现了 **5 个 Critical 缺陷**，全部是计划代码里的错。其中至少 2 个在被发现时**测试是全绿的**：

- `exec.js` 超时只杀直接子进程，孙进程持有管道导致 Promise 永不 settle
- `security.js` 令牌长度预检比字符数、比较比字节数，特定请求头使 401 变 500

之后我偷懒跳过了评审，连续 5 笔提交、820 行改动没有任何独立复核。补做评审时又查出：`checkOrigin` 传字符串会退化成子串匹配（`Origin: http` 即可放行，fail-open）、pip 的 `requested` 断言为假使依赖包永不显示「依赖」警告、30 处变异中 13 处存活。

**结论：测试全绿不构成保证，独立评审才是发现 Critical 的机制。**

## 规矩

### 一、变更前

**新功能必须先定要件，再动手。** 顺序是 要件定义 → 仕様 → 実装，不是反过来。

判别标准：如果这次改动会让 `docs/specs/` 里的要件书发生变化（新增要件、改变 Item 结构、改变错误码、改变安全边界），那它就是新功能，必须先写要件。

违反的例子：中文简介功能是先实装、推送，事后才把 FR-24 / FR-25 补进要件书。这导致要件被写成了「描述既有实装」，失去约束力。

**需求有两种合理解读时，先问，不要猜。** 猜对了是运气，不是流程。

### 二、变更中

- **TDD**：先写会失败的测试，跑一遍确认它真的失败，再实装。「确认失败」这一步不能跳 —— 它是在验证测试本身有效。
- **不硬编码本机事实**。写死包名、版本、路径、数量之前先问：换一台机器还成立吗？
- **命令的真实输出要实测**，不要凭记忆写解析逻辑。`brew list --versions` 取最后一个 token 曾经就是这么错的。
- **测试固件用真实输出**。虚构的固件会把错误行为钉死 —— `openssl@3 3.4.0 3.5.1` 这个虚构的升序样例，曾经让「取最后一个」看起来是对的，而真实输出是降序。

### 三、变更后（**最容易被跳过的一步**）

**每一次代码变更都必须经过独立评审，不分大小。** 不是「改得多才评审」。安全边界的三行改动比一百行重构更需要复核。

评审分两段，顺序不能反：

1. **规格评审** —— 实装的是不是要求的东西？有没有多做、少做、理解偏了？
2. **质量评审** —— 实装得好不好？边界、错误路径、契约、可维护性。

评审者必须是**独立的**，不能是写这段代码的同一个上下文。评审要求：

- **不信任实装者的报告**，自己读代码、自己跑验证
- **给出实证**，不是推理。「我觉得这里会挂」和「我构造了孙进程持有管道的场景，实测 5 秒未 settle」是两回事
- **做变异测试**：把实装改坏，看测试会不会失败。存活的变异 = 没有保护的行为

### 四、测试的判据

**新测试必须能杀死对应的变异。** 写完测试后自己改坏实装验证一遍。

警惕这类恒真测试：

- 只断言「新字段存在」「新常量被定义」，不验证「新语义被正确使用」。拆分了 `LIST_MAX_OUTPUT_BYTES` 和 `ACTION_MAX_OUTPUT_BYTES`，却没有一条测试验证适配器用对了哪个 —— 把两者写反，测试全绿
- `deepEqual(buildConfig(), DEFAULTS)` 在实装错误地直接返回 `DEFAULTS` 本身时同样通过

### 五、破坏性操作

这个面板能卸载软件。涉及以下内容的改动，评审规格从严：

- `server/security.js` 的任何改动
- `server/exec.js` 的子进程执行与超时
- 适配器中生成命令参数的函数（`actionArgs`）
- `routes.js` 的校验顺序

**不要替使用者执行真实的破坏性命令**。验证只用只读命令。需要验证失败路径时，停掉服务再点击，而不是真的跑 `brew uninstall`。

### 六、不要动使用者的进程

使用者可能正在自己的终端运行本服务（端口 7788）。

- **禁止 `pkill -f "node server/index.js"`** —— 宽匹配会杀掉他的进程。这个错误已经犯过一次
- 需要起服务验证时，用 `PORT=7799` 的临时 `.env`，以记录的 PID 精确终止，结束后删除该 `.env`
- **禁止在前台运行 `npm start`** —— 会一直挂住

### 七、文档同步

改了这些就必须同步 `docs/specs/` 的要件书：

- Item 结构（7.2 节）
- 错误码（7.6 节）
- 配置项（4.2 节）
- 安全边界（第 5 章）

要件书里的**论证理由也要正确**，不只是结论正确。「localhost 是浏览器保留名必定解析到回环」这个理由是错的 —— 真正的理由是 Origin 反映发起文档的源。错误的理由会让后人据此推出错误的结论。

## 检查清单

每次变更提交前自问：

- [ ] 这是新功能吗？如果是，要件先写了吗？
- [ ] 测试是先写的吗？确认过它真的会失败吗？
- [ ] 独立评审跑了吗？规格评审和质量评审都跑了吗？
- [ ] 新测试能杀死对应的变异吗？验证过吗？
- [ ] 要件书需要同步吗？
- [ ] 有没有动到使用者的进程或系统状态？

**任何一项答不上来，就是没做完。**
